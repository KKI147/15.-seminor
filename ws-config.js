/**
 * 멀티플레이 WebSocket 주소 (선택)
 * 비워 두면: npm start 로 연 페이지와 같은 호스트·포트 사용 (권장)
 * Vercel(정적) + Render(WS) 분리 시 예:
 *   window.TETRIS_WS_URL = 'wss://your-app.onrender.com';
 */
(function () {
    window.TETRIS_WS_URL = '';
})();
